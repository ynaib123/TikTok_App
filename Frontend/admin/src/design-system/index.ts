/**
 * Phase 2.7 — Design system primitives.
 *
 * Wraps the existing journey-* CSS classes so consumers don't have to
 * remember class names. Add new primitives here as patterns surface
 * (Input, Card, Toast, etc.).
 */
export { Button } from './Button'
export { Modal } from './Modal'
export { Pill } from './Pill'
export { Spinner } from './Spinner'
