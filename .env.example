# Default for local dev (uses Vite proxy + Docker nginx in prod)
# Copy to .env.local only if you need a custom IP (mobile testing).
VITE_API_BASE_URL=/api

# Example for mobile testing on the same Wi-Fi:
# VITE_API_BASE_URL=http://192.168.11.100:8081/api
